{% embed '@SalesReturnGui/_partials/column.twig' %}
    {% block title %}
        <div class="clearfix">
            <h5>{{ 'Total' | trans }}</h5>
            <h5 class="float-end">{{ return.returnItems | length | executeFilterIfExists('formatInt', app.locale) ~ ' ' ~ 'items' | trans }}</h5>
        </div>
    {% endblock %}

    {% block content %}
        {% set currencyIsoCode = (return.returnItems | first).orderItem.currencyIsoCode %}

        <div class="row">
            <div class="col-9 font-bold">
                {{ 'Remuneration Total' | trans ~ ':' }}
            </div>
            <div class="col-3 text-right">
                {{ return.returnTotals.remunerationTotal | money(true, currencyIsoCode) }}
            </div>
        </div>
    {% endblock %}
{% endembed %}
